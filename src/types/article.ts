export interface Article {
  title: string
  description: string
  date: string
  category: string
  slug: string
  content?: string
}

export interface ArticleMetadata {
  title: string
  description: string
  date: string
  category: string
  slug: string
}
